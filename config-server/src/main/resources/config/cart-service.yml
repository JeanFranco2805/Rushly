server:
  port: 8050
  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka/
      http-client:
        type: java

spring:
  cloud:
    discovery:
      enabled: true